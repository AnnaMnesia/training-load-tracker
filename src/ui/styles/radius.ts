// The radius scale (simple & intentional)
// Four values:
// sm → inputs, chips
// md → buttons
// lg → cards
// xl → large panels / modals

export const radius = {
  sm: 6,
  md: 8,
  lg: 12,
  xl: 16,
} as const;
